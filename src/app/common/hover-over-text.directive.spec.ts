import { HoverOverTextDirective } from './hover-over-text.directive';

describe('HoverOverTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverOverTextDirective();
    expect(directive).toBeTruthy();
  });
});
