import { DecodeUrlPipe } from './decode-url.pipe';

describe('DecodeUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
