export class SearchResult {
  bolId: string;
  bolNumber: string;
  externalBol: Boolean;
  itemId: string;
  requesterId: string;
  recipient: string;
  pickupLocation: string;
  deliveryLocation: string;
  cargoType: string;
  createdDate: Date;
  submittedDate: Date;
  pickedDate: Date;
  deliveredDate: Date;
  receivedBy: string;
  comment: string;
  partInfo: string;
  completed: Boolean;
}
